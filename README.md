# soil-moisture-webapp
A simple website built using HTML, CSS and JavaScript and uses chart.js and opensheet to display the moisture readings and whether it needs to be watered or not. It receives data from a capacitive soil moisture sensor connected to an esp32 over Wi-Fi. The data is first written into a google sheet, from where it is extracted using the Opensheet API.
